{
  "name": "@newchat/db",
  "private": true,
  "version": "0.0.0",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/schema.ts"
  },
  "scripts": {
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:push": "drizzle-kit push",
    "db:nuke": "bun run src/scripts/nuke.ts --confirm"
  },
  "dependencies": {
    "drizzle-orm": "^0.38",
    "postgres": "^3.4.9"
  },
  "devDependencies": {
    "@newchat/typescript-config": "workspace:*",
    "drizzle-kit": "^0.30"
  }
}
